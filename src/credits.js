/* Cutscene stub. The public demo has no in-run credits set-piece; no level
 * carries the data that would trigger it. Exports preserved as no-ops. */
export const WAKE_DUR = 2.6;
export function startCredits() {}
export function updateCredits() { return true; }
export function drawCredits() {}
export function drawWaking() {}
