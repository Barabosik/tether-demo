/* Ending stub. The public demo has no campaign finale (isCampaignFinale is
 * false here), so this never triggers. Exports preserved as no-ops. */
export function startFinale() {}
export function updateFinale() { return true; }
export function finaleAdvance() { return false; }
export function drawFinale() {}
